// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.7
//   protoc               unknown
// source: byx/lojas/v1/params.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "byx.lojas.v1";

/** Parâmetros do módulo lojas */
export interface Params {
  /** Endereço bech32 que pode chamar o faucet (opcional) */
  faucetAdmin: string;
  /** Liga/desliga o faucet no módulo */
  faucetEnabled: boolean;
  /** Limite por transação do faucet em ubyx (Int em string, ex "1000000000" = 1000 BYX) */
  faucetMaxPerTx: string;
  /**
   * Taxa de cashback em ubyx por R$1.
   * Ex.: 2500 = 0.0025 BYX/R$1 = 2500 ubyx/R$1.
   */
  cashbackRateUbyxPerReal: number;
  /** Limite máximo de valor de venda em centavos (anti-abuso) */
  maxValorVendaEmCentavos: number;
  /** Limite máximo de cashback por venda em ubyx. */
  maxCashbackUbyxPorVenda: number;
  /** Limite diário de cashback por loja em ubyx. */
  maxCashbackDailyPerLojaUbyx: number;
  /** Limite de vendas por bloco por loja */
  maxSalesPerBlockPerLoja: number;
  /** Habilita/desabilita verificações de limite */
  limitsEnabled: boolean;
  /** Quantos dias manter acumulador diário antes de limpar (MVP simples) */
  cashbackDailyRetentionDays: number;
}

function createBaseParams(): Params {
  return {
    faucetAdmin: "",
    faucetEnabled: false,
    faucetMaxPerTx: "",
    cashbackRateUbyxPerReal: 0,
    maxValorVendaEmCentavos: 0,
    maxCashbackUbyxPorVenda: 0,
    maxCashbackDailyPerLojaUbyx: 0,
    maxSalesPerBlockPerLoja: 0,
    limitsEnabled: false,
    cashbackDailyRetentionDays: 0,
  };
}

export const Params: MessageFns<Params> = {
  encode(message: Params, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.faucetAdmin !== "") {
      writer.uint32(10).string(message.faucetAdmin);
    }
    if (message.faucetEnabled !== false) {
      writer.uint32(16).bool(message.faucetEnabled);
    }
    if (message.faucetMaxPerTx !== "") {
      writer.uint32(26).string(message.faucetMaxPerTx);
    }
    if (message.cashbackRateUbyxPerReal !== 0) {
      writer.uint32(32).uint64(message.cashbackRateUbyxPerReal);
    }
    if (message.maxValorVendaEmCentavos !== 0) {
      writer.uint32(40).uint64(message.maxValorVendaEmCentavos);
    }
    if (message.maxCashbackUbyxPorVenda !== 0) {
      writer.uint32(48).uint64(message.maxCashbackUbyxPorVenda);
    }
    if (message.maxCashbackDailyPerLojaUbyx !== 0) {
      writer.uint32(56).uint64(message.maxCashbackDailyPerLojaUbyx);
    }
    if (message.maxSalesPerBlockPerLoja !== 0) {
      writer.uint32(64).uint32(message.maxSalesPerBlockPerLoja);
    }
    if (message.limitsEnabled !== false) {
      writer.uint32(72).bool(message.limitsEnabled);
    }
    if (message.cashbackDailyRetentionDays !== 0) {
      writer.uint32(80).uint32(message.cashbackDailyRetentionDays);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Params {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseParams();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.faucetAdmin = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.faucetEnabled = reader.bool();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.faucetMaxPerTx = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.cashbackRateUbyxPerReal = longToNumber(reader.uint64());
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.maxValorVendaEmCentavos = longToNumber(reader.uint64());
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.maxCashbackUbyxPorVenda = longToNumber(reader.uint64());
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.maxCashbackDailyPerLojaUbyx = longToNumber(reader.uint64());
          continue;
        }
        case 8: {
          if (tag !== 64) {
            break;
          }

          message.maxSalesPerBlockPerLoja = reader.uint32();
          continue;
        }
        case 9: {
          if (tag !== 72) {
            break;
          }

          message.limitsEnabled = reader.bool();
          continue;
        }
        case 10: {
          if (tag !== 80) {
            break;
          }

          message.cashbackDailyRetentionDays = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Params {
    return {
      faucetAdmin: isSet(object.faucetAdmin)
        ? globalThis.String(object.faucetAdmin)
        : isSet(object.faucet_admin)
        ? globalThis.String(object.faucet_admin)
        : "",
      faucetEnabled: isSet(object.faucetEnabled)
        ? globalThis.Boolean(object.faucetEnabled)
        : isSet(object.faucet_enabled)
        ? globalThis.Boolean(object.faucet_enabled)
        : false,
      faucetMaxPerTx: isSet(object.faucetMaxPerTx)
        ? globalThis.String(object.faucetMaxPerTx)
        : isSet(object.faucet_max_per_tx)
        ? globalThis.String(object.faucet_max_per_tx)
        : "",
      cashbackRateUbyxPerReal: isSet(object.cashbackRateUbyxPerReal)
        ? globalThis.Number(object.cashbackRateUbyxPerReal)
        : isSet(object.cashback_rate_ubyx_per_real)
        ? globalThis.Number(object.cashback_rate_ubyx_per_real)
        : 0,
      maxValorVendaEmCentavos: isSet(object.maxValorVendaEmCentavos)
        ? globalThis.Number(object.maxValorVendaEmCentavos)
        : isSet(object.max_valor_venda_em_centavos)
        ? globalThis.Number(object.max_valor_venda_em_centavos)
        : 0,
      maxCashbackUbyxPorVenda: isSet(object.maxCashbackUbyxPorVenda)
        ? globalThis.Number(object.maxCashbackUbyxPorVenda)
        : isSet(object.max_cashback_ubyx_por_venda)
        ? globalThis.Number(object.max_cashback_ubyx_por_venda)
        : 0,
      maxCashbackDailyPerLojaUbyx: isSet(object.maxCashbackDailyPerLojaUbyx)
        ? globalThis.Number(object.maxCashbackDailyPerLojaUbyx)
        : isSet(object.max_cashback_daily_per_loja_ubyx)
        ? globalThis.Number(object.max_cashback_daily_per_loja_ubyx)
        : 0,
      maxSalesPerBlockPerLoja: isSet(object.maxSalesPerBlockPerLoja)
        ? globalThis.Number(object.maxSalesPerBlockPerLoja)
        : isSet(object.max_sales_per_block_per_loja)
        ? globalThis.Number(object.max_sales_per_block_per_loja)
        : 0,
      limitsEnabled: isSet(object.limitsEnabled)
        ? globalThis.Boolean(object.limitsEnabled)
        : isSet(object.limits_enabled)
        ? globalThis.Boolean(object.limits_enabled)
        : false,
      cashbackDailyRetentionDays: isSet(object.cashbackDailyRetentionDays)
        ? globalThis.Number(object.cashbackDailyRetentionDays)
        : isSet(object.cashback_daily_retention_days)
        ? globalThis.Number(object.cashback_daily_retention_days)
        : 0,
    };
  },

  toJSON(message: Params): unknown {
    const obj: any = {};
    if (message.faucetAdmin !== "") {
      obj.faucetAdmin = message.faucetAdmin;
    }
    if (message.faucetEnabled !== false) {
      obj.faucetEnabled = message.faucetEnabled;
    }
    if (message.faucetMaxPerTx !== "") {
      obj.faucetMaxPerTx = message.faucetMaxPerTx;
    }
    if (message.cashbackRateUbyxPerReal !== 0) {
      obj.cashbackRateUbyxPerReal = Math.round(message.cashbackRateUbyxPerReal);
    }
    if (message.maxValorVendaEmCentavos !== 0) {
      obj.maxValorVendaEmCentavos = Math.round(message.maxValorVendaEmCentavos);
    }
    if (message.maxCashbackUbyxPorVenda !== 0) {
      obj.maxCashbackUbyxPorVenda = Math.round(message.maxCashbackUbyxPorVenda);
    }
    if (message.maxCashbackDailyPerLojaUbyx !== 0) {
      obj.maxCashbackDailyPerLojaUbyx = Math.round(message.maxCashbackDailyPerLojaUbyx);
    }
    if (message.maxSalesPerBlockPerLoja !== 0) {
      obj.maxSalesPerBlockPerLoja = Math.round(message.maxSalesPerBlockPerLoja);
    }
    if (message.limitsEnabled !== false) {
      obj.limitsEnabled = message.limitsEnabled;
    }
    if (message.cashbackDailyRetentionDays !== 0) {
      obj.cashbackDailyRetentionDays = Math.round(message.cashbackDailyRetentionDays);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Params>, I>>(base?: I): Params {
    return Params.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Params>, I>>(object: I): Params {
    const message = createBaseParams();
    message.faucetAdmin = object.faucetAdmin ?? "";
    message.faucetEnabled = object.faucetEnabled ?? false;
    message.faucetMaxPerTx = object.faucetMaxPerTx ?? "";
    message.cashbackRateUbyxPerReal = object.cashbackRateUbyxPerReal ?? 0;
    message.maxValorVendaEmCentavos = object.maxValorVendaEmCentavos ?? 0;
    message.maxCashbackUbyxPorVenda = object.maxCashbackUbyxPorVenda ?? 0;
    message.maxCashbackDailyPerLojaUbyx = object.maxCashbackDailyPerLojaUbyx ?? 0;
    message.maxSalesPerBlockPerLoja = object.maxSalesPerBlockPerLoja ?? 0;
    message.limitsEnabled = object.limitsEnabled ?? false;
    message.cashbackDailyRetentionDays = object.cashbackDailyRetentionDays ?? 0;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function longToNumber(int64: { toString(): string }): number {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
