// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.6.1
//   protoc               unknown
// source: byx/lojas/v1/params.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "byx.lojas.v1";

/** Parâmetros do módulo lojas */
export interface Params {
  /** Endereço bech32 que pode chamar o faucet (opcional) */
  faucetAdmin: string;
  /** Liga/desliga o faucet no módulo */
  faucetEnabled: boolean;
  /** Limite por transação do faucet (Int em string, ex "1000") */
  faucetMaxPerTx: string;
  /** Cashback em micro BYX por real gasto (ex.: 2500 = 0.0025 BYX/R$1) */
  cashbackRateMicroByxPerReal: number;
  /** Limite máximo de valor de venda em centavos (anti-abuso) */
  maxValorVendaEmCentavos: number;
  /** Limite máximo de cashback por venda em micro BYX */
  maxCashbackMicroByxPorVenda: number;
  /** Limite diário de cashback por loja (micro BYX) */
  maxCashbackDailyPerLojaMicrobyx: number;
  /** Limite de vendas por bloco por loja */
  maxSalesPerBlockPerLoja: number;
  /** Habilita/desabilita verificações de limite */
  limitsEnabled: boolean;
  /** Quantos dias manter acumulador diário antes de limpar (MVP simples) */
  cashbackDailyRetentionDays: number;
}

function createBaseParams(): Params {
  return {
    faucetAdmin: "",
    faucetEnabled: false,
    faucetMaxPerTx: "",
    cashbackRateMicroByxPerReal: 0,
    maxValorVendaEmCentavos: 0,
    maxCashbackMicroByxPorVenda: 0,
    maxCashbackDailyPerLojaMicrobyx: 0,
    maxSalesPerBlockPerLoja: 0,
    limitsEnabled: false,
    cashbackDailyRetentionDays: 0,
  };
}

export const Params: MessageFns<Params> = {
  encode(message: Params, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.faucetAdmin !== "") {
      writer.uint32(10).string(message.faucetAdmin);
    }
    if (message.faucetEnabled !== false) {
      writer.uint32(16).bool(message.faucetEnabled);
    }
    if (message.faucetMaxPerTx !== "") {
      writer.uint32(26).string(message.faucetMaxPerTx);
    }
    if (message.cashbackRateMicroByxPerReal !== 0) {
      writer.uint32(32).uint64(message.cashbackRateMicroByxPerReal);
    }
    if (message.maxValorVendaEmCentavos !== 0) {
      writer.uint32(40).uint64(message.maxValorVendaEmCentavos);
    }
    if (message.maxCashbackMicroByxPorVenda !== 0) {
      writer.uint32(48).uint64(message.maxCashbackMicroByxPorVenda);
    }
    if (message.maxCashbackDailyPerLojaMicrobyx !== 0) {
      writer.uint32(56).uint64(message.maxCashbackDailyPerLojaMicrobyx);
    }
    if (message.maxSalesPerBlockPerLoja !== 0) {
      writer.uint32(64).uint32(message.maxSalesPerBlockPerLoja);
    }
    if (message.limitsEnabled !== false) {
      writer.uint32(72).bool(message.limitsEnabled);
    }
    if (message.cashbackDailyRetentionDays !== 0) {
      writer.uint32(80).uint32(message.cashbackDailyRetentionDays);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Params {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseParams();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.faucetAdmin = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.faucetEnabled = reader.bool();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.faucetMaxPerTx = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.cashbackRateMicroByxPerReal = longToNumber(reader.uint64());
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.maxValorVendaEmCentavos = longToNumber(reader.uint64());
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.maxCashbackMicroByxPorVenda = longToNumber(reader.uint64());
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.maxCashbackDailyPerLojaMicrobyx = longToNumber(reader.uint64());
          continue;
        }
        case 8: {
          if (tag !== 64) {
            break;
          }

          message.maxSalesPerBlockPerLoja = reader.uint32();
          continue;
        }
        case 9: {
          if (tag !== 72) {
            break;
          }

          message.limitsEnabled = reader.bool();
          continue;
        }
        case 10: {
          if (tag !== 80) {
            break;
          }

          message.cashbackDailyRetentionDays = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Params {
    return {
      faucetAdmin: isSet(object.faucetAdmin) ? globalThis.String(object.faucetAdmin) : "",
      faucetEnabled: isSet(object.faucetEnabled) ? globalThis.Boolean(object.faucetEnabled) : false,
      faucetMaxPerTx: isSet(object.faucetMaxPerTx) ? globalThis.String(object.faucetMaxPerTx) : "",
      cashbackRateMicroByxPerReal: isSet(object.cashbackRateMicroByxPerReal)
        ? globalThis.Number(object.cashbackRateMicroByxPerReal)
        : 0,
      maxValorVendaEmCentavos: isSet(object.maxValorVendaEmCentavos)
        ? globalThis.Number(object.maxValorVendaEmCentavos)
        : 0,
      maxCashbackMicroByxPorVenda: isSet(object.maxCashbackMicroByxPorVenda)
        ? globalThis.Number(object.maxCashbackMicroByxPorVenda)
        : 0,
      maxCashbackDailyPerLojaMicrobyx: isSet(object.maxCashbackDailyPerLojaMicrobyx)
        ? globalThis.Number(object.maxCashbackDailyPerLojaMicrobyx)
        : 0,
      maxSalesPerBlockPerLoja: isSet(object.maxSalesPerBlockPerLoja)
        ? globalThis.Number(object.maxSalesPerBlockPerLoja)
        : 0,
      limitsEnabled: isSet(object.limitsEnabled) ? globalThis.Boolean(object.limitsEnabled) : false,
      cashbackDailyRetentionDays: isSet(object.cashbackDailyRetentionDays)
        ? globalThis.Number(object.cashbackDailyRetentionDays)
        : 0,
    };
  },

  toJSON(message: Params): unknown {
    const obj: any = {};
    if (message.faucetAdmin !== "") {
      obj.faucetAdmin = message.faucetAdmin;
    }
    if (message.faucetEnabled !== false) {
      obj.faucetEnabled = message.faucetEnabled;
    }
    if (message.faucetMaxPerTx !== "") {
      obj.faucetMaxPerTx = message.faucetMaxPerTx;
    }
    if (message.cashbackRateMicroByxPerReal !== 0) {
      obj.cashbackRateMicroByxPerReal = Math.round(message.cashbackRateMicroByxPerReal);
    }
    if (message.maxValorVendaEmCentavos !== 0) {
      obj.maxValorVendaEmCentavos = Math.round(message.maxValorVendaEmCentavos);
    }
    if (message.maxCashbackMicroByxPorVenda !== 0) {
      obj.maxCashbackMicroByxPorVenda = Math.round(message.maxCashbackMicroByxPorVenda);
    }
    if (message.maxCashbackDailyPerLojaMicrobyx !== 0) {
      obj.maxCashbackDailyPerLojaMicrobyx = Math.round(message.maxCashbackDailyPerLojaMicrobyx);
    }
    if (message.maxSalesPerBlockPerLoja !== 0) {
      obj.maxSalesPerBlockPerLoja = Math.round(message.maxSalesPerBlockPerLoja);
    }
    if (message.limitsEnabled !== false) {
      obj.limitsEnabled = message.limitsEnabled;
    }
    if (message.cashbackDailyRetentionDays !== 0) {
      obj.cashbackDailyRetentionDays = Math.round(message.cashbackDailyRetentionDays);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Params>, I>>(base?: I): Params {
    return Params.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Params>, I>>(object: I): Params {
    const message = createBaseParams();
    message.faucetAdmin = object.faucetAdmin ?? "";
    message.faucetEnabled = object.faucetEnabled ?? false;
    message.faucetMaxPerTx = object.faucetMaxPerTx ?? "";
    message.cashbackRateMicroByxPerReal = object.cashbackRateMicroByxPerReal ?? 0;
    message.maxValorVendaEmCentavos = object.maxValorVendaEmCentavos ?? 0;
    message.maxCashbackMicroByxPorVenda = object.maxCashbackMicroByxPorVenda ?? 0;
    message.maxCashbackDailyPerLojaMicrobyx = object.maxCashbackDailyPerLojaMicrobyx ?? 0;
    message.maxSalesPerBlockPerLoja = object.maxSalesPerBlockPerLoja ?? 0;
    message.limitsEnabled = object.limitsEnabled ?? false;
    message.cashbackDailyRetentionDays = object.cashbackDailyRetentionDays ?? 0;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function longToNumber(int64: { toString(): string }): number {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
