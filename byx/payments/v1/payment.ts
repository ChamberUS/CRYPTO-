// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.6.1
//   protoc               unknown
// source: byx/payments/v1/payment.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "byx.payments.v1";

/** PaymentStatus indica o estado do pedido de pagamento. */
export enum PaymentStatus {
  PAYMENT_STATUS_UNSPECIFIED = 0,
  PAYMENT_STATUS_PENDING = 1,
  PAYMENT_STATUS_PAID = 2,
  PAYMENT_STATUS_EXPIRED = 3,
  PAYMENT_STATUS_CANCELED = 4,
  UNRECOGNIZED = -1,
}

export function paymentStatusFromJSON(object: any): PaymentStatus {
  switch (object) {
    case 0:
    case "PAYMENT_STATUS_UNSPECIFIED":
      return PaymentStatus.PAYMENT_STATUS_UNSPECIFIED;
    case 1:
    case "PAYMENT_STATUS_PENDING":
      return PaymentStatus.PAYMENT_STATUS_PENDING;
    case 2:
    case "PAYMENT_STATUS_PAID":
      return PaymentStatus.PAYMENT_STATUS_PAID;
    case 3:
    case "PAYMENT_STATUS_EXPIRED":
      return PaymentStatus.PAYMENT_STATUS_EXPIRED;
    case 4:
    case "PAYMENT_STATUS_CANCELED":
      return PaymentStatus.PAYMENT_STATUS_CANCELED;
    case -1:
    case "UNRECOGNIZED":
    default:
      return PaymentStatus.UNRECOGNIZED;
  }
}

export function paymentStatusToJSON(object: PaymentStatus): string {
  switch (object) {
    case PaymentStatus.PAYMENT_STATUS_UNSPECIFIED:
      return "PAYMENT_STATUS_UNSPECIFIED";
    case PaymentStatus.PAYMENT_STATUS_PENDING:
      return "PAYMENT_STATUS_PENDING";
    case PaymentStatus.PAYMENT_STATUS_PAID:
      return "PAYMENT_STATUS_PAID";
    case PaymentStatus.PAYMENT_STATUS_EXPIRED:
      return "PAYMENT_STATUS_EXPIRED";
    case PaymentStatus.PAYMENT_STATUS_CANCELED:
      return "PAYMENT_STATUS_CANCELED";
    case PaymentStatus.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

/** PaymentRequest representa um pedido de pagamento criado por uma loja. */
export interface PaymentRequest {
  id: number;
  lojaId: number;
  amountMicrobyx: number;
  memo: string;
  status: PaymentStatus;
  createdAtUnix: number;
  expiresAtUnix: number;
  payer: string;
  paidAtUnix: number;
}

function createBasePaymentRequest(): PaymentRequest {
  return {
    id: 0,
    lojaId: 0,
    amountMicrobyx: 0,
    memo: "",
    status: 0,
    createdAtUnix: 0,
    expiresAtUnix: 0,
    payer: "",
    paidAtUnix: 0,
  };
}

export const PaymentRequest: MessageFns<PaymentRequest> = {
  encode(message: PaymentRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== 0) {
      writer.uint32(8).uint64(message.id);
    }
    if (message.lojaId !== 0) {
      writer.uint32(16).uint64(message.lojaId);
    }
    if (message.amountMicrobyx !== 0) {
      writer.uint32(24).uint64(message.amountMicrobyx);
    }
    if (message.memo !== "") {
      writer.uint32(34).string(message.memo);
    }
    if (message.status !== 0) {
      writer.uint32(40).int32(message.status);
    }
    if (message.createdAtUnix !== 0) {
      writer.uint32(48).uint64(message.createdAtUnix);
    }
    if (message.expiresAtUnix !== 0) {
      writer.uint32(56).uint64(message.expiresAtUnix);
    }
    if (message.payer !== "") {
      writer.uint32(66).string(message.payer);
    }
    if (message.paidAtUnix !== 0) {
      writer.uint32(72).uint64(message.paidAtUnix);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PaymentRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePaymentRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.id = longToNumber(reader.uint64());
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.lojaId = longToNumber(reader.uint64());
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.amountMicrobyx = longToNumber(reader.uint64());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.memo = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.status = reader.int32() as any;
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.createdAtUnix = longToNumber(reader.uint64());
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.expiresAtUnix = longToNumber(reader.uint64());
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.payer = reader.string();
          continue;
        }
        case 9: {
          if (tag !== 72) {
            break;
          }

          message.paidAtUnix = longToNumber(reader.uint64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PaymentRequest {
    return {
      id: isSet(object.id) ? globalThis.Number(object.id) : 0,
      lojaId: isSet(object.lojaId) ? globalThis.Number(object.lojaId) : 0,
      amountMicrobyx: isSet(object.amountMicrobyx) ? globalThis.Number(object.amountMicrobyx) : 0,
      memo: isSet(object.memo) ? globalThis.String(object.memo) : "",
      status: isSet(object.status) ? paymentStatusFromJSON(object.status) : 0,
      createdAtUnix: isSet(object.createdAtUnix) ? globalThis.Number(object.createdAtUnix) : 0,
      expiresAtUnix: isSet(object.expiresAtUnix) ? globalThis.Number(object.expiresAtUnix) : 0,
      payer: isSet(object.payer) ? globalThis.String(object.payer) : "",
      paidAtUnix: isSet(object.paidAtUnix) ? globalThis.Number(object.paidAtUnix) : 0,
    };
  },

  toJSON(message: PaymentRequest): unknown {
    const obj: any = {};
    if (message.id !== 0) {
      obj.id = Math.round(message.id);
    }
    if (message.lojaId !== 0) {
      obj.lojaId = Math.round(message.lojaId);
    }
    if (message.amountMicrobyx !== 0) {
      obj.amountMicrobyx = Math.round(message.amountMicrobyx);
    }
    if (message.memo !== "") {
      obj.memo = message.memo;
    }
    if (message.status !== 0) {
      obj.status = paymentStatusToJSON(message.status);
    }
    if (message.createdAtUnix !== 0) {
      obj.createdAtUnix = Math.round(message.createdAtUnix);
    }
    if (message.expiresAtUnix !== 0) {
      obj.expiresAtUnix = Math.round(message.expiresAtUnix);
    }
    if (message.payer !== "") {
      obj.payer = message.payer;
    }
    if (message.paidAtUnix !== 0) {
      obj.paidAtUnix = Math.round(message.paidAtUnix);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PaymentRequest>, I>>(base?: I): PaymentRequest {
    return PaymentRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PaymentRequest>, I>>(object: I): PaymentRequest {
    const message = createBasePaymentRequest();
    message.id = object.id ?? 0;
    message.lojaId = object.lojaId ?? 0;
    message.amountMicrobyx = object.amountMicrobyx ?? 0;
    message.memo = object.memo ?? "";
    message.status = object.status ?? 0;
    message.createdAtUnix = object.createdAtUnix ?? 0;
    message.expiresAtUnix = object.expiresAtUnix ?? 0;
    message.payer = object.payer ?? "";
    message.paidAtUnix = object.paidAtUnix ?? 0;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function longToNumber(int64: { toString(): string }): number {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
