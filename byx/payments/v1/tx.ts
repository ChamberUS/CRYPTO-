// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.6.1
//   protoc               unknown
// source: byx/payments/v1/tx.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Params } from "./params";
import { PaymentRequest } from "./payment";

export const protobufPackage = "byx.payments.v1";

/** MsgUpdateParams is the Msg/UpdateParams request type. */
export interface MsgUpdateParams {
  /** authority is the address that controls the module (defaults to x/gov unless overwritten). */
  authority: string;
  /**
   * params defines the module parameters to update.
   *
   * NOTE: All parameters must be supplied.
   */
  params: Params | undefined;
}

/**
 * MsgUpdateParamsResponse defines the response structure for executing a
 * MsgUpdateParams message.
 */
export interface MsgUpdateParamsResponse {
}

/** MsgCreatePaymentRequest cria um pedido de pagamento para uma loja. */
export interface MsgCreatePaymentRequest {
  creator: string;
  lojaId: number;
  amountMicrobyx: number;
  memo: string;
  expiresInSeconds: number;
}

export interface MsgCreatePaymentRequestResponse {
  id: number;
}

/** MsgPayPaymentRequest permite um pagador liquidar um pedido pendente. */
export interface MsgPayPaymentRequest {
  creator: string;
  requestId: number;
}

export interface MsgPayPaymentRequestResponse {
  paymentRequest: PaymentRequest | undefined;
}

function createBaseMsgUpdateParams(): MsgUpdateParams {
  return { authority: "", params: undefined };
}

export const MsgUpdateParams: MessageFns<MsgUpdateParams> = {
  encode(message: MsgUpdateParams, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.authority !== "") {
      writer.uint32(10).string(message.authority);
    }
    if (message.params !== undefined) {
      Params.encode(message.params, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MsgUpdateParams {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgUpdateParams();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.authority = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.params = Params.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MsgUpdateParams {
    return {
      authority: isSet(object.authority) ? globalThis.String(object.authority) : "",
      params: isSet(object.params) ? Params.fromJSON(object.params) : undefined,
    };
  },

  toJSON(message: MsgUpdateParams): unknown {
    const obj: any = {};
    if (message.authority !== "") {
      obj.authority = message.authority;
    }
    if (message.params !== undefined) {
      obj.params = Params.toJSON(message.params);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<MsgUpdateParams>, I>>(base?: I): MsgUpdateParams {
    return MsgUpdateParams.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<MsgUpdateParams>, I>>(object: I): MsgUpdateParams {
    const message = createBaseMsgUpdateParams();
    message.authority = object.authority ?? "";
    message.params = (object.params !== undefined && object.params !== null)
      ? Params.fromPartial(object.params)
      : undefined;
    return message;
  },
};

function createBaseMsgUpdateParamsResponse(): MsgUpdateParamsResponse {
  return {};
}

export const MsgUpdateParamsResponse: MessageFns<MsgUpdateParamsResponse> = {
  encode(_: MsgUpdateParamsResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MsgUpdateParamsResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgUpdateParamsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): MsgUpdateParamsResponse {
    return {};
  },

  toJSON(_: MsgUpdateParamsResponse): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<MsgUpdateParamsResponse>, I>>(base?: I): MsgUpdateParamsResponse {
    return MsgUpdateParamsResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<MsgUpdateParamsResponse>, I>>(_: I): MsgUpdateParamsResponse {
    const message = createBaseMsgUpdateParamsResponse();
    return message;
  },
};

function createBaseMsgCreatePaymentRequest(): MsgCreatePaymentRequest {
  return { creator: "", lojaId: 0, amountMicrobyx: 0, memo: "", expiresInSeconds: 0 };
}

export const MsgCreatePaymentRequest: MessageFns<MsgCreatePaymentRequest> = {
  encode(message: MsgCreatePaymentRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.creator !== "") {
      writer.uint32(10).string(message.creator);
    }
    if (message.lojaId !== 0) {
      writer.uint32(16).uint64(message.lojaId);
    }
    if (message.amountMicrobyx !== 0) {
      writer.uint32(24).uint64(message.amountMicrobyx);
    }
    if (message.memo !== "") {
      writer.uint32(34).string(message.memo);
    }
    if (message.expiresInSeconds !== 0) {
      writer.uint32(40).uint64(message.expiresInSeconds);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MsgCreatePaymentRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgCreatePaymentRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.creator = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.lojaId = longToNumber(reader.uint64());
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.amountMicrobyx = longToNumber(reader.uint64());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.memo = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.expiresInSeconds = longToNumber(reader.uint64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MsgCreatePaymentRequest {
    return {
      creator: isSet(object.creator) ? globalThis.String(object.creator) : "",
      lojaId: isSet(object.lojaId) ? globalThis.Number(object.lojaId) : 0,
      amountMicrobyx: isSet(object.amountMicrobyx) ? globalThis.Number(object.amountMicrobyx) : 0,
      memo: isSet(object.memo) ? globalThis.String(object.memo) : "",
      expiresInSeconds: isSet(object.expiresInSeconds) ? globalThis.Number(object.expiresInSeconds) : 0,
    };
  },

  toJSON(message: MsgCreatePaymentRequest): unknown {
    const obj: any = {};
    if (message.creator !== "") {
      obj.creator = message.creator;
    }
    if (message.lojaId !== 0) {
      obj.lojaId = Math.round(message.lojaId);
    }
    if (message.amountMicrobyx !== 0) {
      obj.amountMicrobyx = Math.round(message.amountMicrobyx);
    }
    if (message.memo !== "") {
      obj.memo = message.memo;
    }
    if (message.expiresInSeconds !== 0) {
      obj.expiresInSeconds = Math.round(message.expiresInSeconds);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<MsgCreatePaymentRequest>, I>>(base?: I): MsgCreatePaymentRequest {
    return MsgCreatePaymentRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<MsgCreatePaymentRequest>, I>>(object: I): MsgCreatePaymentRequest {
    const message = createBaseMsgCreatePaymentRequest();
    message.creator = object.creator ?? "";
    message.lojaId = object.lojaId ?? 0;
    message.amountMicrobyx = object.amountMicrobyx ?? 0;
    message.memo = object.memo ?? "";
    message.expiresInSeconds = object.expiresInSeconds ?? 0;
    return message;
  },
};

function createBaseMsgCreatePaymentRequestResponse(): MsgCreatePaymentRequestResponse {
  return { id: 0 };
}

export const MsgCreatePaymentRequestResponse: MessageFns<MsgCreatePaymentRequestResponse> = {
  encode(message: MsgCreatePaymentRequestResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== 0) {
      writer.uint32(8).uint64(message.id);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MsgCreatePaymentRequestResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgCreatePaymentRequestResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.id = longToNumber(reader.uint64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MsgCreatePaymentRequestResponse {
    return { id: isSet(object.id) ? globalThis.Number(object.id) : 0 };
  },

  toJSON(message: MsgCreatePaymentRequestResponse): unknown {
    const obj: any = {};
    if (message.id !== 0) {
      obj.id = Math.round(message.id);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<MsgCreatePaymentRequestResponse>, I>>(base?: I): MsgCreatePaymentRequestResponse {
    return MsgCreatePaymentRequestResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<MsgCreatePaymentRequestResponse>, I>>(
    object: I,
  ): MsgCreatePaymentRequestResponse {
    const message = createBaseMsgCreatePaymentRequestResponse();
    message.id = object.id ?? 0;
    return message;
  },
};

function createBaseMsgPayPaymentRequest(): MsgPayPaymentRequest {
  return { creator: "", requestId: 0 };
}

export const MsgPayPaymentRequest: MessageFns<MsgPayPaymentRequest> = {
  encode(message: MsgPayPaymentRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.creator !== "") {
      writer.uint32(10).string(message.creator);
    }
    if (message.requestId !== 0) {
      writer.uint32(16).uint64(message.requestId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MsgPayPaymentRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgPayPaymentRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.creator = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.requestId = longToNumber(reader.uint64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MsgPayPaymentRequest {
    return {
      creator: isSet(object.creator) ? globalThis.String(object.creator) : "",
      requestId: isSet(object.requestId) ? globalThis.Number(object.requestId) : 0,
    };
  },

  toJSON(message: MsgPayPaymentRequest): unknown {
    const obj: any = {};
    if (message.creator !== "") {
      obj.creator = message.creator;
    }
    if (message.requestId !== 0) {
      obj.requestId = Math.round(message.requestId);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<MsgPayPaymentRequest>, I>>(base?: I): MsgPayPaymentRequest {
    return MsgPayPaymentRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<MsgPayPaymentRequest>, I>>(object: I): MsgPayPaymentRequest {
    const message = createBaseMsgPayPaymentRequest();
    message.creator = object.creator ?? "";
    message.requestId = object.requestId ?? 0;
    return message;
  },
};

function createBaseMsgPayPaymentRequestResponse(): MsgPayPaymentRequestResponse {
  return { paymentRequest: undefined };
}

export const MsgPayPaymentRequestResponse: MessageFns<MsgPayPaymentRequestResponse> = {
  encode(message: MsgPayPaymentRequestResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.paymentRequest !== undefined) {
      PaymentRequest.encode(message.paymentRequest, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MsgPayPaymentRequestResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgPayPaymentRequestResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.paymentRequest = PaymentRequest.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MsgPayPaymentRequestResponse {
    return {
      paymentRequest: isSet(object.paymentRequest) ? PaymentRequest.fromJSON(object.paymentRequest) : undefined,
    };
  },

  toJSON(message: MsgPayPaymentRequestResponse): unknown {
    const obj: any = {};
    if (message.paymentRequest !== undefined) {
      obj.paymentRequest = PaymentRequest.toJSON(message.paymentRequest);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<MsgPayPaymentRequestResponse>, I>>(base?: I): MsgPayPaymentRequestResponse {
    return MsgPayPaymentRequestResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<MsgPayPaymentRequestResponse>, I>>(object: I): MsgPayPaymentRequestResponse {
    const message = createBaseMsgPayPaymentRequestResponse();
    message.paymentRequest = (object.paymentRequest !== undefined && object.paymentRequest !== null)
      ? PaymentRequest.fromPartial(object.paymentRequest)
      : undefined;
    return message;
  },
};

/** Msg defines the Msg service. */
export interface Msg {
  UpdateParams(request: MsgUpdateParams): Promise<MsgUpdateParamsResponse>;
  /** Cria um novo pedido de pagamento para a loja informada. */
  CreatePaymentRequest(request: MsgCreatePaymentRequest): Promise<MsgCreatePaymentRequestResponse>;
  /** Paga um pedido pendente. */
  PayPaymentRequest(request: MsgPayPaymentRequest): Promise<MsgPayPaymentRequestResponse>;
}

export const MsgServiceName = "byx.payments.v1.Msg";
export class MsgClientImpl implements Msg {
  private readonly rpc: Rpc;
  private readonly service: string;
  constructor(rpc: Rpc, opts?: { service?: string }) {
    this.service = opts?.service || MsgServiceName;
    this.rpc = rpc;
    this.UpdateParams = this.UpdateParams.bind(this);
    this.CreatePaymentRequest = this.CreatePaymentRequest.bind(this);
    this.PayPaymentRequest = this.PayPaymentRequest.bind(this);
  }
  UpdateParams(request: MsgUpdateParams): Promise<MsgUpdateParamsResponse> {
    const data = MsgUpdateParams.encode(request).finish();
    const promise = this.rpc.request(this.service, "UpdateParams", data);
    return promise.then((data) => MsgUpdateParamsResponse.decode(new BinaryReader(data)));
  }

  CreatePaymentRequest(request: MsgCreatePaymentRequest): Promise<MsgCreatePaymentRequestResponse> {
    const data = MsgCreatePaymentRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "CreatePaymentRequest", data);
    return promise.then((data) => MsgCreatePaymentRequestResponse.decode(new BinaryReader(data)));
  }

  PayPaymentRequest(request: MsgPayPaymentRequest): Promise<MsgPayPaymentRequestResponse> {
    const data = MsgPayPaymentRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "PayPaymentRequest", data);
    return promise.then((data) => MsgPayPaymentRequestResponse.decode(new BinaryReader(data)));
  }
}

interface Rpc {
  request(service: string, method: string, data: Uint8Array): Promise<Uint8Array>;
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function longToNumber(int64: { toString(): string }): number {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
