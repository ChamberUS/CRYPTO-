# Mock Merchant (BYX / IAOS)

Servidor HTTP local para receber webhooks do `webhook-relay`.

## Rodar
```bash
cd integrations/mock-merchant
npm install
MERCHANT_WEBHOOK_SECRET="dev_secret_123" npm run dev
```
